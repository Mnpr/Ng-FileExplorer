const fs = require('fs');
const url = require('url')
const path = require('path');

/**
 *  Folder Class
 * 
 * # Methods 
 * - Create()
 * - Delete()
 * - Rename()
 * - Move() | Cut() | Copy()
 * - Select()
 */


class Folder {
    constructor(public name:string ) {
    
    }
    
}
